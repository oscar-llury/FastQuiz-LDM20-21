<resources>
    <!-- Base Theme -->
    <!-- Light base theme-->
    <style name="FastQuizTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/colorAccent_light</item>
        <item name="backgroundColor">@color/background_light</item>

        <item name="primaryTextColor">@color/primaryTextColor_light</item>
        <item name="secondaryTextColor">@color/primaryDarkColor</item>

        <item name="dividerColor">#F2F2F3</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>
    <!-- Dark base theme -->
    <style name="darkTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/colorAccent_dark</item>
        <item name="backgroundColor">@color/background_dark</item>

        <item name="primaryTextColor">@color/primaryTextColor_dark</item>
        <item name="secondaryTextColor">@color/primaryDarkColor</item>

        <item name="dividerColor">#F2F2F3</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

</resources>
